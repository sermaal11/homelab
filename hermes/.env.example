HERMES_GATEWAY_PORT=8642
HERMES_DASHBOARD_PORT=9119
HERMES_DATA_DIR=/data/homelab/hermes/data
HERMES_API_SERVER_KEY=change_me_generate_with_openssl_rand_hex_32
HERMES_MODEL=gpt-5.5
HERMES_DASHBOARD_INSECURE=1

# Model/provider credentials. OpenAI Codex OAuth is configured inside Hermes.
# Groq is still used by Honcho memory, but Hermes has no fallback provider by default.
OPENROUTER_API_KEY=
OPENAI_API_KEY=
GROQ_API_KEY=

# Optional: Telegram can also be configured inside Hermes after first boot.
TELEGRAM_BOT_TOKEN=
TELEGRAM_ALLOWED_USERS=
TELEGRAM_HOME_CHANNEL=

# Service URLs exposed to Hermes for its own skills/tools. These are not MCPs.
HONCHO_BASE_URL=http://honcho-api:8000
HERMES_SELF_URL=http://localhost:8642
HOMEASSISTANT_URL=http://host.docker.internal:8123
N8N_URL=http://n8n:5678
NEXTCLOUD_URL=http://nextcloud
PROMETHEUS_URL=http://prometheus:9090
GRAFANA_URL=http://grafana:3000

# Honcho memory sidecars run inside the Hermes stack.
# Honcho is built from source because upstream does not publish a Docker Hub image.
# Embeddings are generated locally by the honcho-ollama sidecar, so Honcho does
# not need a paid embedding provider for memory documents.
HONCHO_GIT_REF=main
HONCHO_API_BIND=127.0.0.1
HONCHO_API_PORT=8000
HONCHO_POSTGRES_PASSWORD=change_me_generate_with_openssl_rand_hex_32
HONCHO_POSTGRES_DATA_DIR=/data/homelab/hermes/honcho/postgres
HONCHO_REDIS_DATA_DIR=/data/homelab/hermes/honcho/redis
HONCHO_AUTH_USE_AUTH=false
HONCHO_LOG_LEVEL=INFO
HONCHO_OPENAI_BASE_URL=https://api.groq.com/openai/v1
HONCHO_GROQ_MODEL=meta-llama/llama-4-scout-17b-16e-instruct
HONCHO_EMBED_MESSAGES=false
HONCHO_DERIVER_FLUSH_ENABLED=true
HONCHO_EMBEDDING_MODEL=nomic-embed-text
HONCHO_EMBEDDING_API_KEY=ollama
HONCHO_EMBEDDING_BASE_URL=http://honcho-ollama:11434/v1
HONCHO_EMBEDDING_VECTOR_DIMENSIONS=768
HONCHO_OLLAMA_DATA_DIR=/data/homelab/hermes/honcho/ollama

TZ=Europe/Madrid
